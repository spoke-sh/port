# Improve Operator Signal And Documentation Experience - Decision Log

<!-- Append entries below. Each entry is an H2 with ISO timestamp. -->
<!-- Use `keel mission digest` to compress older entries when this file grows large. -->

## 2026-03-11T13:09:00

Created mission `VDaiFelPf` to track the operator-signal cleanup after the
hosted-service hardening mission closed. The workstream will land one
verification-focused `just mission` entrypoint, simplify the top-level `just`
and CLI help surfaces, and publish root-level documentation contracts for Port.
